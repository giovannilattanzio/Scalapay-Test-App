# Tasks

## 1. The OpenAPI document

- [x] 1.1 [tooling] Write `openapi/catalog-api.yaml` (OpenAPI 3.0.3) with the server, the `/v1/products/search` operation (`operationId: searchProducts`, `tags: [Products]`) and its eleven query parameters, typed and with the defaults 1 and 30 on paging, price bounds optional, as in design.md — Contracts; verify the document validates against the OpenAPI 3 schema (spec `catalog-api-contract` — The contract is described as an OpenAPI document / Every request parameter is described)
- [x] 1.2 [tooling] Add the response schemas — `ProductSearchResponse`, `GroupedHit`, `Hit`, `ProductDocument`, `FacetCount`, `FacetValue`, `FacetStats`, `DetectedMerchant` — typing every price and statistic as `number` so the service's int/decimal mix parses, marking `merchantDetectedFromQuery` nullable and `has_image` an integer; verify the document still validates and that the captured response `test/fixtures/product_search_nike.json` conforms to `ProductSearchResponse` under a schema validator (spec `catalog-api-contract` — The response shape is described in full)
- [x] 1.3 [tooling] Add the measured behaviour as descriptions on the fields it affects: `sort_by` (only `_text_match` and `selling_price` honoured, any other field accepted and ignored, direction inert on relevance), `page` (past the last page the first is returned again), `found` (equals the items returned, not a total), `per_page` (the reachable window moves with it); verify each of the four descriptions is present and names the consequence for a client (spec `catalog-api-contract` — Measured service behaviour is recorded in the document)

## 2. Generation

- [x] 2.1 [tooling] Add `openapi_generator_cli: ^7.0.0` as a dev dependency of the root package and record the generation command in a place a developer will find it; verify `flutter pub get` resolves with no conflict (the `openapi_generator` builder cannot be used — see design.md Decision 4) and that `java -version` reports a JDK
- [x] 2.2 [tooling] Generate the package with `dart run openapi_generator_cli:main generate -i openapi/catalog-api.yaml -g dart-dio -o packages/catalog_api --additional-properties=pubName=catalog_api,serializationLibrary=json_serializable`; verify `packages/catalog_api/lib/src/api/products_api.dart` exposes `searchProducts` with one named argument per query parameter, that the models are annotated `@JsonSerializable` and not `built_value`, and that the generated signature matches the one recorded in design.md — Contracts, including the `source_` spelling (spec `generated-api-client` — The client is generated, not written)
- [x] 2.3 [tooling] Take over `packages/catalog_api/.openapi-generator-ignore` to list `pubspec.yaml` and `analysis_options.yaml`, hand-write both with the app's pinned versions from design.md — Contracts (`publish_to: none`, `resolution: workspace`, `dio: ^5.11.1`, `json_annotation: 4.11.0`, `copy_with_extension: 12.1.0`, dev `json_serializable`, `copy_with_extension_gen`, `build_runner`; lints relaxed for generated sources), and add `packages/catalog_api` to the root `workspace:` list and as a path dependency; verify one `flutter pub get` at the root resolves all four packages and that re-running the generator leaves both files byte-identical (spec `generated-api-client` — Hand-maintained files inside the package survive regeneration)
- [x] 2.4 [tooling] Run `build_runner` inside `packages/catalog_api` to emit the `.g.dart` files (generation is two steps), then commit the whole package and confirm git does not ignore it; verify `dart analyze` is clean inside the package, that regenerating with an unchanged document leaves `git status` clean, and that `flutter analyze` and `flutter test` pass at the root with the new workspace member (specs `generated-api-client` — The client is generated, not written / Building and testing never require the generator)

## 3. Contract test

- [x] 3.1 [data] Add `test/data/catalog/generated_contract_test.dart` parsing `test/fixtures/product_search_nike.json` through the generated `ProductSearchResponse`; verify it asserts the product count, that a whole-number price and a decimal price both become the same numeric type, that the store names match the captured values, that a document carrying `isMerchantCard` parses as an ordinary product, and that a response without `merchantDetectedFromQuery` and with empty facet statistics still parses — with no network access (spec `catalog-api-contract` — The document is kept honest by a test)

## 4. Aligning the sibling change and documenting

- [x] 4.1 [tooling] Revise the planning artifacts of `add-product-catalog-page` so it both consumes the generated client and takes over the app wiring this change cannot perform: drop its tasks 3.2 and 3.3 (hand-written models and data source), reduce 3.1 to the partner constants, point 3.4 at `ProductsApi`, revise its design.md contracts for `lib/src/data/catalog/`, and add tasks for the product mapper, the `CatalogApi`/`ProductsApi` DI registrations reusing the configured `Dio`, the `CatalogApi` name collision rename, and the check that no file under `lib/src/domain/` or `lib/src/presentation/` imports `package:catalog_api` — each citing the `generated-api-client` requirements "The app depends on the client only through the data layer" and "The client shares the application's configured transport", which this change states but does not implement; verify `openspec validate add-product-catalog-page --strict` passes, that no task there still names `ProductModel` or `ProductRemoteDataSourceImpl`, and that both requirements are cited by at least one task
- [x] 4.2 [tooling] Document in `README.md` how to regenerate the client (the two-step command, the Java requirement, that the generated package is committed and never hand-edited, and which files inside it are hand-maintained), state that `openapi/catalog-api.yaml` is reverse-engineered from the live service on a given date rather than vendor-supplied, with the note that refreshing the fixture is a manual step, and record why the document declares a single host: the assessment document's second host `catalog-api.dev.scalapay.com` was tried and answers **401** to the identical request with `{"code":"AuthorizationNotFound","message":"Authorization header not found"}`, so it is the same API behind an `Authorization` header — but the scheme is not stated, there is no `WWW-Authenticate` header to infer it from, no token came with the assessment and nothing documents how to obtain one, so it was left out of `servers` rather than given a guessed security scheme that no one could test — and adding it later is one entry, with no effect on the generated code; verify the documented commands run as written and leave the tree clean (spec `generated-api-client` — Building and testing never require the generator)
- [x] 4.3 [tooling] Run `dart format lib test`, `flutter analyze` and `flutter test` at the root and in `packages/scalapay_ui`, excluding the generated package from formatting; verify all are clean
